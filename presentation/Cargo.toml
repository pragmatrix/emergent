[package]
name = "emergent-presentation"
version = "0.1.0"
authors = ["Armin Sander <armin@replicator.org>"]
edition = "2018"

[dependencies]
emergent-drawing = { path = "../drawing" }
serde = { version = "1.0.99", features = ["derive"] }
